{
  "name": "entropy-report-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Static HTML report hub for Entropy Makeup.",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js"
  },
  "engines": {
    "node": ">=18"
  }
}
